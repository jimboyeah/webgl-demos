export default WebGLUtils;
declare namespace WebGLUtils {
    export { create3DContext };
    export { setupWebGL };
}
/**
 * Creates a webgl context.
 * @param {!Canvas} canvas The canvas tag to get context
 *     from. If one is not passed in one will be created.
 * @return {!WebGLContext} The created context.
 */
declare function create3DContext(canvas: any, opt_attribs: any): any;
/**
 * Creates a webgl context. If creation fails it will
 * change the contents of the container of the <canvas>
 * tag to an error message with the correct links for WebGL.
 * @param {Element} canvas. The canvas element to create a
 *     context from.
 * @param {WebGLContextCreationAttirbutes} opt_attribs Any
 *     creation attributes you want to pass in.
 * @param {function:(msg)} opt_onError An function to call
 *     if there is an error during creation.
 * @return {WebGLRenderingContext} The created context.
 */
declare function setupWebGL(canvas: any, opt_attribs: any, opt_onError: any): WebGLRenderingContext;
